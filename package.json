{
  "name": "divergify-website",
  "private": true,
  "version": "0.1.0",
  "description": "Divergify marketing site (static, Netlify-hosted).",
  "scripts": {
    "build": "node -e \"console.log('Static site: no build step required')\""
  },
  "engines": {
    "node": ">=18"
  }
}
